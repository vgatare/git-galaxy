{
  "name": "git-galaxy",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Turn any GitHub repository into an interactive 3D galaxy.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
